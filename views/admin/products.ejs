<%- include( '../_layouts/adminheader') %>

<h2 class="page-title">Product</h2>
<a href="/admin/products/add-product" class="btn btn-primary">Add a new product </a>
<br><br>
<% if(count > 0) {%>
  this<%=count%>
<table class="table table-striped ">
  <thead>
<tr class="Home" {
  constructor() {

  }
}>

<th> Product </th>
<th> Price </th>
<th> Category </th>
<th> Product image </th>
<th> Edit </th>
<th> Delete </th>
</tr>
  </thead>
  <tbody>
    <% products.forEach(function(product){ %>
      <tr>
        <td ><%= product.title %></td>
        <td >$<%=parseFloat(product.  price).toFixed(2)%></td>
        <td ><%= product.category %></td>
        <td >
          <% if(product.image == ""){%>
            <img id= "noimage" src="../images/noimage.png" >
        <%  } else {  %>
            <img id="noimage" src="../product_images/<%= product._id%>/<%= product.image%>" alt="myimage" >
      <%  }%>
        </td>
        <td><a href="/admin/products/edit-product/<%= product._id %>">Edit</a></td>
            <td><a href="/admin/products/delete-product/<%= product._id %>">Delete</a></td>
        </tr>

  <%   }) ; %>

  </tbody>
</table>
<% } else {%>
  <h3 class="text-center">There are no products  </h3>
  <h1>count is <%=count%></h1>
  <% }%>
<%- include( '../_layouts/adminfooter') %>
